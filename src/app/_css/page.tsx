import React from 'react'

export default function CssPage() {
  return (
    <div>
      Css Page
    </div>
  )
}
// when you want to add folder inside app folder, and don't want it to be route, you have to add _ before the folder name
// so it will not be route, but it will be a folder